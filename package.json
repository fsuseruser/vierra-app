{
  "name": "vierra-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix server && npm install --prefix client",
    "build": "npm run install:all && npm run build --prefix client",
    "start": "npm run prisma:migrate --prefix server && node server/src/index.js",
    "seed": "node server/prisma/seed.js",
    "dev:server": "node server/src/index.js",
    "dev:client": "npm run dev --prefix client"
  }
}